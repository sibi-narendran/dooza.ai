import FINDemoClient from './FINDemoClient';

export const metadata = {
  title: 'FIN Lead Finder — Demo',
  robots: 'noindex, nofollow',
};

export default function FINDemoPage() {
  return <FINDemoClient />;
}
