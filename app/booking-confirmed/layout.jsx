export const metadata = {
    title: 'Booking Confirmed - Dooza',
    description: 'Your free setup call with the founder is confirmed.',
    robots: {
        index: false,
        follow: false,
    },
};

export default function BookingConfirmedLayout({ children }) {
    return children;
}
